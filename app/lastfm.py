"""Last.fm client — similar artists + top tracks via pylast."""
# artist.getSimilar(artist, limit=30)
# artist.getTopTracks(artist, limit=5)
# Returns list of (artist_name, title, playcount, listeners, tags[], year)
